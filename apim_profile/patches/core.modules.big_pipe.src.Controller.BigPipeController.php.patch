--- core/modules/big_pipe/src/Controller/BigPipeController.php	2026-05-06 15:19:19
+++ core/modules/big_pipe/src/Controller/BigPipeController.php	2026-05-15 16:20:20
@@ -49,10 +49,14 @@
     if (!$request->query->has('destination')) {
       throw new HttpException(400, 'The original location is missing.');
     }
+    $destination = $request->query->get('destination');
+    if (isset($GLOBALS['real_base_path']) && strpos($destination, $GLOBALS['real_base_path']) !== 0) {
+      $destination = $GLOBALS['real_base_path'] . $destination;
+    }
 
-    $response = new LocalRedirectResponse($request->query->get('destination'));
+    $response = new LocalRedirectResponse($destination);
     // Set cookie without httpOnly, so that JavaScript can delete it.
-    $response->headers->setCookie(new Cookie(BigPipeStrategy::NOJS_COOKIE, TRUE, 0, '/', NULL, FALSE, FALSE, FALSE, NULL));
+    $response->headers->setCookie(new Cookie(BigPipeStrategy::NOJS_COOKIE, TRUE, 0, '/', NULL, TRUE, FALSE, FALSE, NULL));
     $response->addCacheableDependency((new CacheableMetadata())->addCacheContexts([
       'cookies:' . BigPipeStrategy::NOJS_COOKIE,
       'session.exists',
