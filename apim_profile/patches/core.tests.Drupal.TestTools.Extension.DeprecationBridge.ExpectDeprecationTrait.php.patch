--- core/tests/Drupal/TestTools/Extension/DeprecationBridge/ExpectDeprecationTrait.php	2026-08-19 12:06:15
+++ core/tests/Drupal/TestTools/Extension/DeprecationBridge/ExpectDeprecationTrait.php	2026-08-19 12:06:15
@@ -41,6 +41,10 @@
     $this->expectUserDeprecationMessageMatches($this->regularExpressionForFormatDescription('%A' . $message . '%A'));
   }
 
+  /**
+   * Checks that the current error handler is not already a TestErrorHandler
+   * before wrapping, to prevent double-wrapping.
+   */
   private function regularExpressionForFormatDescription(string $string): string {
     $string = strtr(preg_quote($string, '/'), [
       '%%' => '%',
